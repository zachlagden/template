name: Lint & Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Cancel in-progress runs for the same branch/PR
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ─── JavaScript / TypeScript ───────────────────────────────────────
  # Uncomment this job for JS/TS projects
  #
  # lint-test-js:
  #   name: Lint & Test (JS/TS)
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #
  #     - uses: pnpm/action-setup@v4
  #       with:
  #         version: 9
  #
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: 22
  #         cache: pnpm
  #
  #     - run: pnpm install --frozen-lockfile
  #     - run: pnpm lint
  #     - run: pnpm test

  # ─── Python ────────────────────────────────────────────────────────
  # Uncomment this job for Python projects
  #
  # lint-test-python:
  #   name: Lint & Test (Python)
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #
  #     - uses: actions/setup-python@v5
  #       with:
  #         python-version: "3.12"
  #         cache: pip
  #
  #     - run: pip install -r requirements.txt
  #     - run: pip install ruff pytest
  #     - run: ruff check .
  #     - run: pytest

  # ─── Rust ──────────────────────────────────────────────────────────
  # Uncomment this job for Rust projects
  #
  # lint-test-rust:
  #   name: Lint & Test (Rust)
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #
  #     - uses: dtolnay/rust-toolchain@stable
  #       with:
  #         components: clippy, rustfmt
  #
  #     - uses: Swatinem/rust-cache@v2
  #
  #     - run: cargo fmt --check
  #     - run: cargo clippy -- -D warnings
  #     - run: cargo test

  # ─── Placeholder ───────────────────────────────────────────────────
  # Remove this job once you've uncommented one of the above
  placeholder:
    name: Template (remove me)
    runs-on: ubuntu-latest
    steps:
      - run: echo "Uncomment the lint-test job for your stack and delete this placeholder."
